The entire UDP part (classes) are marked as internal as it's expirmental and still under development.
For now this library is TCP only.

To include the UDP files part change `PropertyGroup element` in the .csproj file to 'true'